  <div id="share">
    <p>
      Sdílet {$contentType} na:
      {foreach $links as $link}
      <a href="{$link->getShareLink($url, $title)}" data-url="{$url}" data-title="{$title}" id="share-link-{$link->getPlatformName()}">{$link->getSiteName()}</a>
      {if !$iterator->last} {/if}
      {/foreach}
    </p>
  </div>
