{layout "../@layout.latte"}
{block title}Profil{/block}
{block content}
  <h1>{if $profile->group}{$profile->group->singleName}{/if} {$profile->publicname}</h1>
  <p>
  V Nexendrii od {$profile->joined}.<br>
  {if $profile->banned}V současnosti si odpykává zasloužený trest v místní šatlavě.<br>{/if}
  Komentářů: {$profile->comments->count()}
  </p>
{/block}