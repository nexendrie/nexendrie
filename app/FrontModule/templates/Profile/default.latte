{layout "../@layout.latte"}
{block title}Profil{/block}
{block content}
  <h1>{$profile->group->singleName} {$profile->publicname}</h1>
  <p>
  V Nexendrii od {$profile->joinedAt}. Žije ve městě/vesnici {$profile->town->name}.<br>
  {if $profile->banned}V současnosti si odpykává zasloužený trest v místní šatlavě.<br>{/if}
  Článků: {$profile->articles->count()} Komentářů: {$profile->comments->count()}
  </p>
{/block}