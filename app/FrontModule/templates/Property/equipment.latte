{layout "../@layout.latte"}
{block title}Vybavení{/block}
{block content}
  <h1>Vybavení</h1>
  {if $items->count() === 0}
  <p>Nemáš žádné vybavení.</p>
  {else}
  <div n:inner-foreach="$items as $item" id="items">
    {$item->amount}x {$item->item->name} - {$item->item->typeCZ} (Síla: {$item->item->strength}) za {$item->item->priceT} {if !$item->worn}<a n:href="equip! $item->id">Nasadit</a>{else}<a n:href="unequip! $item->id">Sundat</a>{/if} <a n:href="sell! $item->id">Prodat 1 kus</a><br>
  </div>
  {/if}
{/block}